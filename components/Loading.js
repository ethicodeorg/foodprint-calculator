import theme from '../styles/theme';

const Loading = () => (
  <div className="loading">
    Loading...
    <style jsx>{`
      .loading {
        color: #fff;
        margin-top: 100px;
      }
    `}</style>
  </div>
);

export default Loading;
